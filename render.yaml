services:
  - type: web
    name: bfhl-api
    runtime: java
    buildCommand: cd bfhl && mvn clean package -DskipTests
    startCommand: cd bfhl && java -jar target/bfhl-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
